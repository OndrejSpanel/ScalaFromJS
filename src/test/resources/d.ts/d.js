class D {
    f(a) {
        var ds;
        var dn;
        if (true) {
            ds = a[0];
            dn = a.length;
        }
    }
}

/* Scala: // TODO: parse source comments for requirements
require:
dn: Double
ds: String
*/
